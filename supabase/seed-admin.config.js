/**
 * Seeded super-admin identity.
 * Change email / displayName here anytime, then re-run: npm run seed:admin
 *
 * Password is NOT stored here — set SEED_ADMIN_PASSWORD in .env.local
 */
module.exports = {
  email: "levifrancis15@gmail.com",
  displayName: "Super Admin",
};
